import 'package:flutter/cupertino.dart';

class AppConfig {
  const AppConfig._();

  static const localeEn = Locale('en');
  static const supportedLocales = [localeEn];
  static const translationsPath = 'assets/translations';
}
